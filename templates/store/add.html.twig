{# format giao diện thủ công => add từng thông tin trong form #}
{% extends "base.html.twig" %}

{% block body %}
<div class="container col-md-5 text-center mt-3">
   <div class="card p-3 bg-light">
      <h3 class="alert alert-primary">Add store</h3>
      {{ form_start (storeForm) }}
      <div class="form-group">
         <label class="text text-warning h6">Store Name</label>
         {{ form_widget(storeForm.name, {
            'attr' : {
               'class' : 'form-control',
               'placeholder' : 'Enter store name'
            }
         }) }}
      </div>
      <div class="form-group">
         <label class="text text-success h6">Store Image</label>
         {{
            form_widget(storeForm.image, {
               'attr' : {
                  'class' : 'form-control',
                  'placeholder' : 'Enter store image'
               }
            })
         }}
      </div>
      <div class="form-group mt-2">
            <input class="btn btn-outline-danger" type="submit" value="Add">
      </div>
      {{ form_end (storeForm) }}
   </div>
</div>
{% endblock %}